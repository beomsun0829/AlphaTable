import React from 'react';
import './Header.css';

const Header: React.FC = () => {
    return (
        <div id="header">
            <h1>Let Tab</h1>
        </div>
    );
};

export default Header;
